Creates the nginx.conf file in your project's root directory. This configures the NGINX web server to listen on the correct port (8080 for Cloud Run) and, crucially, to handle single-page application routing by redirecting all requests to index.html.
